
O NOME DO SQL ESTÁ DEFINIDO COMO "BibliotecaPOO"

create database bibliotecapoo;
use bibliotecapoo;
drop database bibliotecapoo;

create table livro (
id serial primary key,
titulo text,
autor varchar(100),
sinopse text,
preco double precision,
editora varchar(100),
paginas int,
volume int,
saga varchar(100),
idioma varchar(30),
datapublicacao int,
disponibilidade int,
categoria text,
versao varchar(50),
classificacaoindicativa int,
isbn varchar(30)
);

-- INSERT TEST
insert into livro (titulo, autor, editora) values ("teste", "teste", "teste");

select * from livro;
drop table livro;